# Postgres connection (used by Drizzle + scripts)
# Use the direct postgres:// URL (not the prisma+postgres:// accelerate URL)
SWG_POSTGRES_URL=

# Vercel Blob Storage (from Vercel Blob integration)
SWG_READ_WRITE_TOKEN=

# OpenAI API (for TTS and sentence validation)
OPENAI_API_KEY=
